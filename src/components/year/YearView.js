const m = require("mithril");

const YearView = {
    view: function () {
        return m('p.wait-message', "Le mode année n'est pas encore implémenté.📆 🚀 ")
    }
}

module.exports = YearView;